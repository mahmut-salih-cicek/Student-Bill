<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xmod.firebase_1">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Firebase_1">
        <activity
            android:name=".View.BorcActivity"
            android:exported="false" />
        <activity
            android:name=".View.RegisterActivty"
            android:exported="false" />
        <activity
            android:name=".View.UserDetailActivity"
            android:exported="false" />
        <activity
            android:name=".View.BillingListActivity"
            android:exported="false" />
        <activity
            android:name=".View.ShareActivity"
            android:exported="false" />
        <activity
            android:name=".View.UserActivity"
            android:exported="false" />
        <activity
            android:name=".View.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>